services:
  - type: web
    name: tournament-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    autoDeploy: false
